[MASTER]

init-hook='import sys; BASE_DIR="/home/kuhnm/projects/hidra"; sys.path.append(BASE_DIR + "/src/sender/eventdetectors"); sys.path.append(BASE_DIR + "/src/sender/datafetchers"); sys.path.append(BASE_DIR + "/src/shared")'

[TYPECHECK]

# List of module names for which member attributes should not be checked
ignored-modules = zmq

[MESSAGES CONTROL]

# Disable the message, report, category or checker with the given id(s)
disable = too-many-public-methods, locally-disabled, fixme


[SIMILARITIES]

# Minimum lines number of a similarity.
min-similarity-lines = 25

# Ignore imports when computing similarities.
ignore-imports = yes
