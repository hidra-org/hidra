FROM debian:stretch

RUN apt update && apt install -y \
    dpkg-dev \
    debhelper \
    python \
    dh-python \
    python-dev \
    python-setuptools \
    python3 \
    python3-dev \
    python3-setuptools
